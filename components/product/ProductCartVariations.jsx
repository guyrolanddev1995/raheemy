import React from 'react';
import {Text, View} from 'react-native';

const ProductCartVariations = () => {
    return (
        <View className="w-"></View>
    );
}

export default React.memo(ProductCartVariations)
